[package]
name = "dtparse"
version = "0.1.0"
edition = "2024"

[dependencies]
indexmap = "2.13.0"
serde = { version = "1.0.228", features = ["derive"], optional = true }

[features]
serde = ["indexmap/serde", "dep:serde"]
multithread = []
